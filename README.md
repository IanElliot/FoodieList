# FoodieList v3 — Senior API + SwiftData client

- Download this zip and follow the API README inside `foodielist-api/`.
- SwiftUI client is in `FoodieListClient/` and uses SwiftData for offline list items with API sync.
